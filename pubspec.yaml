name: instrument_definition_converter
description: "A new Flutter package project."
version: 0.0.1
homepage:

environment:
  sdk: ^3.6.0
  flutter: ">=1.17.0"

dependencies:
  args: ^2.6.0
  csv: ^6.0.0

dev_dependencies:

